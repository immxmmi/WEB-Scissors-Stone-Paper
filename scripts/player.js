// Player Class
// eslint-disable-next-line import/prefer-default-export
export class Player {
    constructor(rank, name, win, lost) {
        this.rank = rank;
        this.name = name;
        this.win = win;
        this.lost = lost;
    }
}
